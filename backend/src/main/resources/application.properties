spring.application.name=investor-portal

# Database
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:MelissaFieldstone75LLC}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:Sanvi#38899}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=${JWT_SECRET:MelissaFieldstone75LLCInvestorPortalSecretKey2024!!}

# Mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:vinaycse@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# App
app.frontend-url=${FRONTEND_URL:http://localhost:4200}
app.frontend.url.www=${APP_FRONTEND_URL_WWW:}

server.port=8080
